import type { MonsterId } from '../../types'

/** Region boundary for current monster content; future regions can add a sibling module. */
export const WHISPERING_WOODS_MONSTER_IDS: readonly MonsterId[] = ['forest-wisp', 'thornling', 'stone-root', 'grove-sentinel', 'forest-heart']

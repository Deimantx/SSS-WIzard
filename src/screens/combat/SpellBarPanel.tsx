import { SPELLS } from '../../game/data/spells'
import { SCHOOLS } from '../../game/data/schools'
import { useGameStore } from '../../store/gameStore'
import type { SpellId } from '../../game/types'
import { Button, Card } from '../../components/ui'
import { formatTime } from '../../game/utils'

export function SpellBarPanel() { const combat = useGameStore((state) => state.combat); const progress = useGameStore((state) => state.progress); const activities = useGameStore((state) => state.activities); const cast = useGameStore((state) => state.castSpell); const toggle = useGameStore((state) => state.toggleAutoCast); return <Card title="Spell Bar" className="spell-card"><div className="spell-list">{(Object.keys(SPELLS) as SpellId[]).map((id) => { const spell = SPELLS[id]; const unlocked = progress.unlockedSpells.includes(id); const auto = activities.autoCast[id]; const summary = spell.effect.type === 'damage' ? `${spell.effect.amount} damage` : spell.effect.type === 'heal' ? `${spell.effect.amount} heal` : spell.effect.type === 'barrier' ? `${spell.effect.amount} barrier` : spell.effect.type === 'dot' ? 'Burning · 5 sec' : 'Quickening · 6 sec'; return <div className={`spell-row ${unlocked ? '' : 'locked'}`} key={id}><div className="spell-mini" style={{ color: SCHOOLS[spell.school].color }}>{SCHOOLS[spell.school].glyph}</div><div className="spell-copy"><strong>{spell.name}</strong><small>{unlocked ? `${spell.manaCost} Mana · ${summary} · ${spell.autoCastFocus} Focus` : `Unlock ${SCHOOLS[spell.school].name} Level ${spell.unlockLevel}`}</small></div><Button variant={auto ? 'success' : 'ghost'} disabled={!unlocked || !combat.active} onClick={() => toggle(id)}>{auto ? 'Auto ON' : 'Auto-Cast'}</Button><Button disabled={!unlocked || !combat.active || combat.spellCooldowns[id] > 0} onClick={() => cast(id)}>{combat.spellCooldowns[id] > 0 ? formatTime(combat.spellCooldowns[id]) : 'Cast'}</Button></div> })}</div></Card> }
